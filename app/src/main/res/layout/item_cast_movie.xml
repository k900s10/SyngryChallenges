<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:paddingTop="8dp"
    android:paddingBottom="8dp"
    android:paddingEnd="8dp"
    >

    <com.google.android.material.imageview.ShapeableImageView
        android:id="@+id/siv_movie_poster"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:scaleType="centerCrop"
        android:src="@drawable/movie_cast"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:shapeAppearanceOverlay="@style/Circular"
        app:strokeColor="@color/dark_grey"
        app:strokeWidth="1dp" />

    <TextView
        android:id="@+id/tv_original_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:maxEms="6"
        android:maxLines="1"
        android:ellipsize="end"
        android:text="@string/tv_cast_original_name"
        android:textSize="14sp"
        app:layout_constraintEnd_toEndOf="@+id/siv_movie_poster"
        app:layout_constraintStart_toStartOf="@+id/siv_movie_poster"
        app:layout_constraintTop_toBottomOf="@+id/siv_movie_poster" />

    <TextView
        android:id="@+id/tv_character"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:text="@string/tv_cast_character"
        android:textColor="@color/grey"
        android:maxEms="6"
        android:maxLines="1"
        android:ellipsize="end"
        app:layout_constraintEnd_toEndOf="@+id/siv_movie_poster"
        app:layout_constraintStart_toStartOf="@+id/siv_movie_poster"
        app:layout_constraintTop_toBottomOf="@+id/tv_original_name" />

</androidx.constraintlayout.widget.ConstraintLayout>